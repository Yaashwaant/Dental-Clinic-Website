# Shree Dental Care - SEO Action Plan

Based on the full SEO audit, here is the prioritized action plan to improve indexing, rankings, and local search visibility.

## Priority 1: Critical Fixes (Immediate Action)

### 1. Fix the Brand Title Tag
- **Issue**: The current title is `<title>Dental Republic - Your Smile, Our Priority</title>`.
- **Action**: Update `app/layout.tsx` (or `app/page.tsx` metadata) to reflect the actual brand and local keywords.
- **Recommended Value**: `Shree Dental Care | Best Dentist in Kamothe, Navi Mumbai`

### 2. Implement LocalBusiness Schema (JSON-LD)
- **Issue**: No structured data exists, hurting Local SEO.
- **Action**: Add a `<script type="application/ld+json">` block in `app/layout.tsx` containing `Dentist` or `LocalBusiness` schema with the clinic's name, address, phone number, opening hours, and aggregate rating.

### 3. Add `robots.txt` and `sitemap.xml`
- **Issue**: Search engines lack crawling directions.
- **Action**: Create `app/robots.ts` and `app/sitemap.ts` (Next.js App Router method) to dynamically generate these files. 
- **Bonus**: Include AI crawler disallow rules in `robots.txt` if desired (e.g., `User-agent: GPTBot Disallow: /`).

## Priority 2: High Impact Optimization (Short Term)

### 4. Optimize Meta Description
- **Issue**: Generic description missing local context.
- **Action**: Update metadata description.
- **Recommended Value**: `Trusted multispeciality dental care in Kamothe, Navi Mumbai. Expert treatments including root canals, implants, and pediatric dentistry by Dr. Sheetal Jagtap-Bhise.`

### 5. Improve H1 Heading
- **Issue**: The hero heading is good for UX but weak for SEO.
- **Action**: Update the H1 to include primary keywords naturally without hurting the design.
- **Example**: `Caring, Painless Dental Care in Kamothe for Your Whole Family`

## Priority 3: Technical Improvements (Long Term)

### 6. Next.js Image Optimization
- **Issue**: Standard `<img>` tags are used.
- **Action**: Refactor standard `<img>` tags in heavy components (Hero, Doctors) to use `next/image` (`<Image />`) for automatic WebP formatting and lazy loading. This will significantly improve Core Web Vitals (LCP).

### 7. Canonical Tags
- **Issue**: Missing canonical URL.
- **Action**: Ensure the Next.js metadata object defines a `metadataBase` and absolute URL so canonical tags are generated automatically.

---
*Generated by Agentic SEO Skill*
